#include "main.h"
#include <stdio.h>

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int main(void)
{
	char *i = "EEllen of Troy";
	char *j = "Ellen";
	char *k;

	k = _strstr(s, j);
	printf("%s\n", k);
	return (0);
}
